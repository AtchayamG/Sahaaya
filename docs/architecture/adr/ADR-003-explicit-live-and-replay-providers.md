# ADR-003 — Explicit Live and Replay Providers

**Status:** Accepted

Replay is deterministic, fictional, credential-free, and visibly simulated. Live uses GPT-5.6 with `store: false` and never silently falls back. Provider selection and errors remain visible to the user and tests.

